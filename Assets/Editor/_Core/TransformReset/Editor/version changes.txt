1.3
    - Custom Origin no longer resets itself after code compilation.
	- Added a tag to show whether the reset origin is in its default state or in a custom state

1.2
    - Fixed an input issue when setting values for rotation in the Transform component.
    - Added support for multiple object selection.
	
1.1
    - Cleaning of deprecated editor code.

1.0
    - Package release.